*.iml
.gradle/
/local.properties
/app/google-services.json
/.idea
.DS_Store
/build
**/build
/captures
.externalNativeBuild
.cxx
__pycache__/
*.pyc
.kotlin/
# Every database, not just the live one. `backend/yo.db` alone left
# `backend/yo.db.migrated-20260728` - a real database with real accounts - one `git add -A` away
# from a public repo, and its `-journal`/`-wal` siblings with it.
backend/yo.db*
backend/*.db
backend/*.db-journal
